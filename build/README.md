# build

This directory will host application build and packaging scripts.

Planned contents:

- publish scripts
- signing hooks
- packaging helpers
- CI support files

This area must not attempt to build the Windows 10 PE base.
