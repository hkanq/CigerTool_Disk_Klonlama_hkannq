# Legacy WinPE Runtime

The files under `winpe/files/` are part of the old WinPE-first runtime model.

They are preserved for transitional reference and for the controlled legacy build path under `build/scripts/legacy/`, but they no longer define the target product architecture.

Current status:

- legacy
- not the default startup model
- kept only until the desktop-first live runtime fully replaces it
