# os

This directory will host CigerTool OS integration assets and layout definitions.

Important boundary:

- this is not a WinPE build system
- the Windows 10 PE base is supplied externally by the user

Planned contents:

- startup integration assets
- folder layout definitions
- shell launch contract
- environment-specific configuration
