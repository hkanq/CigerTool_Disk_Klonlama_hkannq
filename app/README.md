# app

This directory will host the CigerTool desktop UI shell.

Planned contents:

- `CigerTool.App`
- views
- view models
- navigation shell
- styling and assets

The app must stay fully graphical and suitable for both normal Windows usage and later WinPE runtime use.
