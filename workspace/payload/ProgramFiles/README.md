# Payload ProgramFiles

Tercih edilen yeni sozlesme:

- `payload/ProgramFiles`

Bu dizindeki icerik build sirasinda workspace icindeki `Program Files` altina merge edilir.

Legacy uyumluluk icin `payload/Program Files` yolu da okunur, ancak yeni eklemeler icin bosluksuz `ProgramFiles` kullanilmalidir.
