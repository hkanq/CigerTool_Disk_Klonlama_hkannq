# Product Vision

CigerTool, boot edilebilir bir USB çalışma alanı ürünüdür.

Final davranış:

- açılış menüsünde `CigerTool Workspace`
- açılış menüsünde `ISO Library`
- varsayılan seçim `CigerTool Workspace`
- workspace seçilince doğrudan hazırlanmış Windows masaüstü
- `CigerTool` otomatik başlangıç
- kullanıcı daha sonra USB'ye ISO bırakabilmeli

Bu repo artık:

- WinPE-first değildir
- Windows Setup-first değildir
- installer deneyi değildir

Bu repo artık:

- hazırlanmış workspace
- dinamik ISO Library
- tek release build girişi

üzerine kuruludur.
